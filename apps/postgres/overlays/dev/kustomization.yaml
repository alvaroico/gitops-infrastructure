apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eos-dev

resources:
  - ../../base

configMapGenerator:
  - name: postgres-config
    behavior: merge
    literals:
      - POSTGRES_DB=eos_dev
